const ROUTES = Object.freeze({
    ROOT: '/',
    BRIDGE: '/bridge',
    POST: '/post/[id]', // example
    COVID: {
        MAIN: '/covid',
        LOGIN: '/covid/login',
    },
})

export default ROUTES
